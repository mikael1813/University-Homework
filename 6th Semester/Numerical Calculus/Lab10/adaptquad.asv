function adaptquad(f,a,b,n,caz)
    if nargin < 5
        caz = 1;
    end
    tol = 0.001;
    if abs(met(f,a,))


end


function output = met(f,a,b,n,caz)
    switch caz
        case 1
            output = algoritm_Simpson(f,a,b,n);
        case 2
            output = algoritm_trapez(f,a,b,n);
        case 3
            output = algoritm_dreptunghi(f,a,b,n);

    end

end