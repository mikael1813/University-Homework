function algoritm_Romberg(f,a,b,n)

    R=[];
    R(1,1) = (b-a)/2*(f(a)+f(b));
    R(2,1) = 1/2*(R(1,1) + (b-a)*f(a+(b-a)/2));
    R(2,2) = (4*R(2,1))

    e=0.1;

    
    while abs(R(i,j)-R(i-1,j-1)) > e
        

    end


end